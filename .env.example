# Enterprise Proxy Configuration (REQUIRED for enterprise environment)
HTTP_PROXY=http://your-proxy-server:port
HTTPS_PROXY=https://your-proxy-server:port

# ConnectChain Configuration (AMEX Enterprise Framework - REQUIRED)
# Path to ConnectChain config file (relative to project root)
CONFIG_PATH=connectchain.config.yml
WORKDIR=.

# ConnectChain EAS (Enterprise Auth Service) credentials
# Uncomment and configure if using EAS authentication
# CONSUMER_ID=your-consumer-id
# CONSUMER_SECRET=your-consumer-secret

# ConnectChain Certificate (if required)
# REQUESTS_CA_BUNDLE=/path/to/ca-bundle.crt

# Google Cloud BigQuery Configuration
GCP_PROJECT_ID=your-gcp-project-id
BIGQUERY_DATASET=your_dataset_name
GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account.json

# Schema Configuration
# Directory containing Excel files (one per table, filename = table name)
SCHEMA_DIRECTORY=/path/to/your/schema_directory

# Optional: Override default settings
# AZURE_OPENAI_API_VERSION=2024-02-15-preview
# BIGQUERY_LOCATION=US
# SESSION_STORAGE_PATH=sessions
